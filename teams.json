{
    "title": "Teams",
    "path": "/teams",
    "methods": {
        "GET": true,
        "POST": false
    },
    "responses": [
        {
            "method": "GET",
            "status_code": 200,
            "status": "OK",
            "content": "HTML for the requested page",
            "context": "Normal operation"
        },
        {
            "method": "GET",
            "status_code": 500,
            "status": "INTERNAL_SERVER_ERROR",
            "content": "Error page",
            "context": "An error occurs when loading team data"
        }
    ],
    "query_params": [
        {
            "name": "confirm",
            "value_type": "bool",
            "required": false,
            "description": "Controls whether to enable confirmation messages for deleting a team. Any value other than `false` will result in the default value `true` being used.",
            "default_value": "true"
        }
    ],
    "logic": {
        "steps": [
            "Gets the confirmation variable from query parameters. If the query parameter is not set to `false`, then the value is set to `true`. Otherwise, the value is to `false`.",
            "Get a list containing every team object from the database.",
            "Render the code for the teams page behind the scenes.",
            "Send the rendered html."
        ],
        "error_handling": "If an error occurs while executing this code, the page will instead render the error page and respond with a status of 500."
    },
    "requireAuth": true,
    "requireMasterAuth": false
}